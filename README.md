# IRP-16-hex-Hash
This is my make-shift hashing algorithm for my independent research project. It takes a text input and outputs a 16-digit hexadecimal hash value. I've implemented pre-image resistance, collision resistance and the avalanche effect as required for standard hash functions. It is important to note that this is NOT computationally secure for proper use
